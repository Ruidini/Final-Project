<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Results</title>
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <h1>Search Results for "<%= searchQuery %>"</h1>

  <% if (pitches.length === 0) { %>
    <p>No pitches found for this location.</p>
  <% } else { %>
    <ul>
      <% pitches.forEach(pitch => { %>
        <li>
          <strong><%= pitch.name %></strong><br>
          Location: <%= pitch.location %><br>
          Type: <%= pitch.type %>
        </li>
      <% }) %>
    </ul>
  <% } %>

  <a href="/">Back to Home</a>
</body>
</html>

